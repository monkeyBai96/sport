{
  "name": "sport-record-mobile",
  "version": "1.0.0",
  "description": "训练记录 App（Capacitor 安卓壳）",
  "scripts": {
    "copy-web": "node copy-web.js",
    "sync": "npm run copy-web && npx cap sync",
    "open": "npx cap open android",
    "build-debug": "npm run sync && cd android && chmod +x gradlew && ./gradlew assembleDebug",
    "build-release": "npm run sync && cd android && chmod +x gradlew && ./gradlew assembleRelease"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
